/** Planned REST surface — single source for UI copy and cURL examples (not hosted yet). */
export const API_V1 = '/api/v1';

export const paths = {
  stations: `${API_V1}/stations`,
  sessions: `${API_V1}/sessions`,
  sessionsStart: `${API_V1}/sessions/start`,
  telemetry: `${API_V1}/telemetry`,
  devices: `${API_V1}/devices`,
  webhooks: `${API_V1}/webhooks`,
};
