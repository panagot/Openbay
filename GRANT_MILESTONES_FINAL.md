# Grant milestones (draft)

Rewrite this file to match your target program (e.g. regional Solana grants). The app today is **OpenBay** — see **[README.md](./README.md)** for scope.

Suggested themes:

1. **Solana Pay** — session invoices; host pubkey settlement.
2. **Telemetry** — Pi / OCPP / Wi-Fi gateway posting signed `meter_wh` or session events.
3. **Public API** — host `GET /stations` + `GET /sessions` matching the API Lab JSON shape.
